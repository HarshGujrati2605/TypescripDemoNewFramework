{
  "name": "suture-playwright",
  "version": "1.0.0",
  "description": "Testcases for Suture health setup",
  "main": "index.js",
  "scripts": {
    "runTestHeaded": "npx playwright test tests/e2e-web/*  --headed",
    "runTestInRuner": "npx playwright test tests/e2e-web/web.login.test  --headed --ui",
    "runLoginTestInChrome" : "npx playwright test tests/e2e-web/web.login.test  --headed --ui --project chrome",
    "rubFilterTestsInChome": "npx playwright test tests/e2e-web/FilterFunctionality.test  --headed --ui --project chrome",
    "runLoginTestsInChome": "npx playwright test tests/e2e-web/web.login.test  --headed --ui --project chrome"

  },
  
  "keywords": [
    "playwright",
    "e2e"
  ],
  "author": "Harsh Gujrati",
  "license": "ISC",
  "devDependencies": {
    "@playwright/test": "^1.44.1",
    "@types/node": "^20.12.12"
  }
}
